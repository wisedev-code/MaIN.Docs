# Shared auth key — must match in both frontend and backend
# DO NOT commit this file with real values
API_KEY=change-me-before-deploy

# Ollama cloud — Code agent (Llama4Scout) + Design agent (Gemma3 12b)
OLLAMA_KEY=
OLLAMA_BASE_URL=https://your-ollama-cloud-endpoint

# OpenAI — Review agent (GPT-4.1)
OPENAI_KEY=
